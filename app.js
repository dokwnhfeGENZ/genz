const posts=[{title:'The internet is becoming a place again',tag:'DIGITAL CULTURE',date:'May 14, 2025',time:'6 min read',desc:'On smaller spaces, slower conversations and the return of the personal web.'},{title:'Notes from a week without notifications',tag:'FIELD NOTES',date:'Apr 28, 2025',time:'4 min read',desc:'What happens when the tiny red badges stop deciding what matters?'},{title:'Designing for the in-between',tag:'MAKING',date:'Apr 09, 2025',time:'8 min read',desc:'A case for interfaces that leave room for ambiguity, rest and surprise.'},{title:'A tiny manifesto for curious people',tag:'ESSAY',date:'Mar 22, 2025',time:'3 min read',desc:'Questions are not a failure state. They are a navigation system.'},{title:'Why everything feels like a dashboard',tag:'CRITICISM',date:'Feb 18, 2025',time:'7 min read',desc:'Looking at the quiet cost of turning every experience into a metric.'},{title:'Building a corner of the web',tag:'MAKING',date:'Jan 30, 2025',time:'5 min read',desc:'A few principles for making digital spaces that feel like yours.'}];
const list=document.querySelector('#articlesList');list.innerHTML=posts.map((p,i)=>`<article class="article-row"><span class="article-number">0${i+1}</span><div><h3>${p.title}</h3><p>${p.tag} · ${p.desc}</p></div><span class="article-meta">${p.date}<br>${p.time}</span></article>`).join('');
const windows=[...document.querySelectorAll('.window')];let z=2;windows.forEach(w=>w.hidden=true);function openWindow(name){windows.forEach(w=>{w.hidden=w.dataset.window!==name;if(!w.hidden)w.style.zIndex=++z});document.querySelectorAll('.dock-item').forEach(x=>x.classList.toggle('selected',x.dataset.open===name))}document.querySelectorAll('[data-open]').forEach(el=>el.addEventListener('click',()=>openWindow(el.dataset.open)));document.querySelectorAll('.close,.minimize').forEach(btn=>btn.addEventListener('click',()=>btn.closest('.window').hidden=true));
const overlay=document.querySelector('#searchOverlay'),input=document.querySelector('#searchInput'),results=document.querySelector('#searchResults');function search(){overlay.hidden=false;input.focus();renderResults('')}function closeSearch(){overlay.hidden=true;input.value=''}function renderResults(q){const found=posts.filter(p=>(p.title+p.tag+p.desc).toLowerCase().includes(q.toLowerCase()));results.innerHTML=found.length?found.map(p=>`<div class="result"><strong>${p.title}</strong><small>${p.tag} · ${p.time}</small></div>`).join(''):'<p class="search-hint">No notes found. Try another search.</p>'}document.querySelector('#menuSearch').addEventListener('click',search);document.querySelector('#dockSearch').addEventListener('click',search);input.addEventListener('input',e=>renderResults(e.target.value));overlay.addEventListener('click',e=>{if(e.target===overlay)closeSearch()});document.addEventListener('keydown',e=>{if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==='k'){e.preventDefault();search()}if(e.key==='Escape')closeSearch()});function tick(){document.querySelector('#clock').textContent=new Date().toLocaleTimeString([], {hour:'2-digit',minute:'2-digit',hour12:false})}tick();setInterval(tick,30000);
